<h1>Edit Staff Details</h1>

<%= form_with(model: @staff, local: true) do |form| %>

  <% if @staff.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@staff.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @staff.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :Name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :Age %><br>
    <%= form.text_field :age %>
  </p>

  <p>
    <%= form.label :Gender %><br>
    <%= form.text_field :gender %>
  </p>

  <p>
    <%= form.label :Designation %><br>
    <%= form.text_field :designation %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>

<td><%= link_to 'Back', staff_path(@staff) %></td>
